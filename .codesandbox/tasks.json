{
  // These tasks will run in order when initializing your CodeSandbox project.
  "setupTasks": ["pip install PyKakao", "pip install gradio"],

  // These tasks can be run from CodeSandbox. Running one will open a log in the app.
  "tasks": {
    "pip install PyKakao": {
      "name": "pip install PyKakao",
      "command": "pip install PyKakao",
      "runAtStart": false
    },
    "pip install gradio": {
      "name": "pip install gradio",
      "command": "pip install gradio",
      "runAtStart": false
    },
    "python3 homepage.py": {
      "name": "python3 homepage.py",
      "command": "python3 homepage.py",
      "runAtStart": true
    }
  }
}
